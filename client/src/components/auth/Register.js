import React,{useState} from 'react'

export default function Register() {
/**
 * This component needs
 * Form inputs to update the post object
 * Form validation
 * axios for handing the request
 * State and dispatch
 * 
 */
const [postObject,setPostObject] = useState({
	fullname:"",
	email:"",
	password:""
})

    return (
        <div>
            
        </div>
    )
}
